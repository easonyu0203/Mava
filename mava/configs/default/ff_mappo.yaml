defaults:
  - logger: ff_mappo_logger
  - arch: anakin
  - system: ppo/ff_mappo
  - network: mlp # [mlp, cnn]
  - env: smax # [cleaner, connector, vector-connector, gigastep, lbf, mabrax, matrax, rware, smax, mpe]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
